# 📡 分流配置库：雷达预警处理手册 (SOP)

当监控到上游规则库发生变动时，会通过 GitHub Issues 或邮件发送预警通知，可用以下流程进行处理。

------------------------------------------------------------

## 🟢 情境一：收到【游戏 / AI 服务新增】预警
**邮件标题示例**：`🎮 [游戏雷达预警] 发现新增 游戏 平台` 或 `🤖 [AI 雷达预警] 发现新增 AI 平台`    
**邮件内容示例**：`发现全新的 游戏 服务被收录！ kurogames @-!cn`

### 📋 应对步骤：无需任何操作 (静默生效)

1. **原理说明**：这代表上游源码刚刚收录了该平台（如库洛游戏 `kurogames`）。系统会自动将其划入 `Game_Proxy` 或 `AI_Rules` 的提取计划中。
2. **验证结果**：
   * 收到邮件后，**忽略该邮件，什么都不用做**。
   * 等待 1-2 天后，随着 GitHub Actions 的日常定时运行，该平台旗下真实的服务器域名（如 `kurogame.com`，而不是标签名 `kurogames`）就会自动出现在 `Game_Proxy.mrs` 或 `.lsr` 规则文件中。

------------------------------------------------------------

## 🟡 情境二：收到【上游新增图纸模块】预警
**邮件标题示例**：`🌐 [新厂雷达预警] 发现上游新增图纸`    
**邮件内容示例**：`发现上游新增图纸模块！ livejournal。 请人工甄别是否加入 Global_CN 列表。`

### 📋 应对步骤：人工评估 + 决定是否修改代码

发现上游新增了一个未知的企业/平台，因为系统无法判断它是否在国内有直连节点，所以需要人工裁决。

#### 第 1 步：人工评估该平台性质
* **情况 A（纯海外网站）**：比如 `livejournal`、`pornhub`、`telegram` 等。它们在中国大陆没有服务器，必须翻墙访问。
* **情况 B（跨国大厂且有国内节点）**：比如未来新增的类似于 `notion`、`figma`、`epicgames` 等。它们虽然是外企，但在国内有 CDN 或合作机房，直连速度比走代理更快。

#### 第 2 步：执行决策
* **若属于情况 A（纯海外网站）**：
  * **操作**：直接忽略该邮件，无需任何操作。系统默认会将未收录的域名丢给你的兜底代理策略（MATCH），正常翻墙访问。
* **若属于情况 B（跨国大厂，需走国内直连）**：
  * **操作**：需要将其加入到 `Global_CN` 白名单代码中。
  * **具体修改流程**：
    1. 打开仓库中的 `.github/workflows/main.yml` 文件。
    2. 找到 `# 🚨 你的心血：Global_CN 跨国大厂国内节点增量提取` 下方的 `for tag in ...` 代码段（约在第 113 行附近）。
    3. 在循环列表的末尾（`do` 的前面），**加上空格并输入该标签的名称**。
       * *修改前*：`... tesla bmw volvo airbnb booking; do`
       * *修改后*：`... tesla bmw volvo airbnb booking 新标签名; do`
    4. 保存并提交 (Commit changes)。
    5. 下一次 Actions 运行时，该跨国大厂的国内节点将被自动提取、洗去广告，并永久合并至你的 `Global_CN` 直连规则库中。
